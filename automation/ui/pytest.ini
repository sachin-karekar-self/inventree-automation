[pytest]
testpaths = tests
addopts = -v --tb=short --browser chromium
markers =
    smoke: login and navigation sanity
    crud: part create/edit/deactivate flows
    crossfunctional: the mandatory multi-step flow
    negative: attribute-gated visibility checks
