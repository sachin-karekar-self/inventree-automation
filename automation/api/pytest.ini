[pytest]
testpaths = tests
addopts = -v --tb=short
markers =
    crud: part CRUD lifecycle
    listing: list/filter/pagination/search
    validation: field-level validation matrix
    categories: part category endpoints
    edge: edge & conflict scenarios
